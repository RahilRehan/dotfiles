# Copy to ~/.config/ai/secrets.env (gitignored) and fill in values.
# Keep credentials here when an optional MCP server needs them. This file is
# never read by the repository's default empty configuration.
